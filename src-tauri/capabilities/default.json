{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the overlay and editor windows",
  "windows": ["overlay", "editor", "colorpick"],
  "permissions": [
    "core:default"
  ]
}
