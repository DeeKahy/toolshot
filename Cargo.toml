[workspace]
resolver = "2"
members = ["daemon", "src-tauri"]

# Screen-sized image work is unusable at opt-level 0, so optimize
# dependencies even in dev builds. Our own crates stay fast to compile.
[profile.dev.package."*"]
opt-level = 2
